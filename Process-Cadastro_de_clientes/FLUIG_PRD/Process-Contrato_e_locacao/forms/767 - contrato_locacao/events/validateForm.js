function validateForm(form){
	var nextState = getValue("WKNextState");
	
	/*if(nextState == 7 || nextState == 9) {
		if (form.getValue("tpContrato") == null || form.getValue("tpContrato") == 0){
			throw "O campo tipo de contrato deve estar preenchido";
		}	
		if (form.getValue("condPagamento") == null || form.getValue("condPagamento") == 0){
			throw "O campo condição de pagamento deve estar preenchido";
		}	
		if (form.getValue("reajContrato") == null || form.getValue("reajContrato") == 0){
			throw "O campo reajusta contrato deve estar preenchido";
		}
		if (form.getValue("tpPlanilha") == null || form.getValue("tpPlanilha") == 0){
			throw "O campo tipo de planilha de contrato deve estar preenchido";
		}
		if (form.getValue("reajPlanilha") == null || form.getValue("reajPlanilha") == 0){
			throw "O campo reajusta planilha deve estar preenchido";
		}
		if (form.getValue("caucao") == null || form.getValue("caucao") == 0){
			throw "O campo caucao deve estar preenchido";
		}
		if (form.getValue("dateInicio") == null || form.getValue("dateInicio") == 0){
			throw "O campo data início deve estar preenchido";
		}
		if (form.getValue("dateFinal") == null || form.getValue("dateFinal") == 0){
			throw "O campo data final deve estar preenchido";
		}
	}*/
	
}