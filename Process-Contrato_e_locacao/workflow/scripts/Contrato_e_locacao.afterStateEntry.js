function afterStateEntry(sequenceId){
	/*var ENVIA_PROPOSTA = 3;
	var atividade = getValue("WKCurrentState");

	if (atividade == ENVIA_PROPOSTA){
		var filial = "OLAA";
		hAPI.setCardValue("filial", filial );	
		
		var dataset = DatasetFactory.getDataset("ws_monta_contrato", null, null, null);
		
		for (var i=0; i < dataset.rowsCount; i++){
			var CCODRET = dataset.getValue(i, "CCODRET");
			var CMSGRET = dataset.getValue(i, "CMSGRET");
			var CNUMCTR = dataset.getValue(i, "CNUMCTR");
			var CREVCTR = dataset.getValue(i, "CREVCTR");
			
			hAPI.setCardValue("orcamento", CCODRET );
			hAPI.setCardValue("proposta", CMSGRET );
			hAPI.setCardValue("vendedor", CNUMCTR );
			hAPI.setCardValue("cliente", CREVCTR );
		}
	}*/
}